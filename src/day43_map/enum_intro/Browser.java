package day43_map.enum_intro;
//enum stands for enumeration
//enum special data type mostly used to define the CONSTANTS
//Usually written in UPPERCASE
//They act like classes so they can have variables(Mostly used for variables) and methods
public enum Browser {

    CHROME, FIREFOX, SAFARI, EDGE

    //public final static int NUMBER = 10;
    //Page object model in selenium
    }
